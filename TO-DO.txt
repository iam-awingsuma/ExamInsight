| for admin users
Menu: Student Manager (Main menu item 4 - after benchmark tests)
    Displays all students from the database table in a tabular format
    *** Searcheable (by Student ID, Name)
    *** buttons/features:
        *** Edit/update student records
        *** Upload student picture/photo

| for non-admin users:
Menu: Data Analysis
** AI integration for intelligent data analysis and interpretation
    *** Individual Student
    *** Class-wise
    *** Year Group

Menu: Reports

Analytics (Dynamic Dashboard)

Previous code: apps/__init.py__

# def create_app(config):

#     # Contextual
#     static_prefix = '/static'
#     # templates_dir = os.path.dirname(config.BASE_DIR)

#     # TEMPLATES_FOLDER = os.path.join(templates_dir,'templates')
#     # STATIC_FOLDER = os.path.join(templates_dir,'static')

#     templates_dir = config.BASE_DIR.parent  # Path
#     TEMPLATES_FOLDER = templates_dir / "templates"
#     STATIC_FOLDER = templates_dir / "static"

#     app = Flask(
#         __name__,
#         static_url_path=static_prefix,
#         template_folder=TEMPLATES_FOLDER,
#         static_folder=STATIC_FOLDER
#     )
#     app.secret_key = "your_secret_key"  # Required for session management

#     app.config.from_object(config)
#     register_extensions(app)
#     register_blueprints(app)

#     return app